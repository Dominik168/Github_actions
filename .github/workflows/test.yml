name: Test Project
on: push
jobs: 
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: action/checkout@v3
      - name: install NodeJS
        uses: actions/setup-node@v3
        with:
          node_version: 18
      - name: Install dependencies
        run: Run tests
        run: npm test
